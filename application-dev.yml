myconfig:
  name: jhpark222232323232555sdsd

spring:
  application:
    name: firstSampleService
  profiles:
    active: dev
  config:
    activate:
      on-profile: dev
    name: firstSampleService
    profile: dev
    import: optional:config-server:http://localhost:8888
    use-legacy-processing: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enable: true


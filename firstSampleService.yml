spring:
  application:
    name: firstSampleService
  profiles:
    active: dev
    
myconfig:
  name: jhpark22223232323222ss2

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enable: true